---
phase: 00-pre-requisite-bug-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - composer_signal_generator/main.py
  - composer_signal_generator/main2.py
  - monte_carlo_sim/Monte Carlo walk forward composer working.py
  - rsi_tester/strategy_engine/src/metrics.py
  - rsi_tester/strategy_engine/src/data_loader.py
  - rsi_tester/strategy_engine/src/data_alignment.py
  - rsi_tester/strategy_engine/src/indicators.py
autonomous: false
requirements:
  - PHASE0-01
  - PHASE0-02
  - PHASE0-03
  - PHASE0-04
  - PHASE0-05
  - PHASE0-06
  - PHASE0-07
  - PHASE0-08
  - PHASE0-09
  - PHASE0-10
  - PHASE0-11

must_haves:
  truths:
    - "The freeze_combo_universe dead code block (lines 3084-3172 of main.py) is gone; no calls to undefined functions remain in that function"
    - "precond_mask is applied per-windowed-slice inside _run_single_backtest, not pre-filtered globally before windowing"
    - "max_workers is read from user input at startup and respected by both ProcessPoolExecutor calls in main.py"
    - "main2.py does not exist"
    - "_ATR() is renamed to _rolling_std() in main.py; _ALLOWED_CALLS maps ROLLING_STD/rolling_std to it; ATR/atr keys are removed"
    - "_BBAND_LOWER() exists in main.py and is registered in _ALLOWED_CALLS as BBAND_LOWER/bband_lower"
    - "rsi_tester data_loader.py saves date,open,high,low,close CSVs; old date,close CSVs still load without error"
    - "load_ticker_csv() returns OHLC columns when present; build_master_dataframe() emits signal_high, signal_low, etc."
    - "calculate_atr() (Wilder ATR) and calculate_bbands_lower() exist in indicators.py and are dispatched by add_indicator()"
    - "Monte Carlo drawdown units are confirmed (either already correct or fixed); plot_drawdown_distributions overlay is visually consistent"
    - "metrics.py Total_Trades counts 0->1 signal transitions; win_rate denominator remains len(active_days)"
  artifacts:
    - path: "composer_signal_generator/main.py"
      provides: "All Group A, B tasks applied"
      contains: "_rolling_std"
    - path: "rsi_tester/strategy_engine/src/indicators.py"
      provides: "calculate_atr and calculate_bbands_lower"
      exports: ["calculate_atr", "calculate_bbands_lower"]
    - path: "rsi_tester/strategy_engine/src/metrics.py"
      provides: "corrected Total_Trades"
      contains: "streak_starts"
    - path: "rsi_tester/strategy_engine/src/data_loader.py"
      provides: "OHLC download"
      contains: "adjHigh"
    - path: "rsi_tester/strategy_engine/src/data_alignment.py"
      provides: "OHLC forwarding"
      contains: "signal_high"
  key_links:
    - from: "data_loader.py download_ticker_data"
      to: "CSV files"
      via: "adjOpen/adjHigh/adjLow/adjClose extraction"
      pattern: "adjHigh.*high"
    - from: "data_alignment.py load_ticker_csv"
      to: "build_master_dataframe"
      via: "OHLC conditional return"
      pattern: "signal_high.*signal_low"
    - from: "indicators.py add_indicator"
      to: "calculate_atr"
      via: "ATR dispatch branch"
      pattern: "indicator_name.upper.*ATR"
    - from: "main.py _run_single_backtest"
      to: "precond_mask"
      via: "7-element tuple unpack + reindex"
      pattern: "precond_mask.reindex"
---

<objective>
Fix all 11 confirmed bugs in the existing codebase before the Phase 1 rewrite begins. This is a pure bug-fix phase — no new features, no architectural changes beyond what is required to fix the listed defects. Every change is surgical and traceable to a specific bug.

Purpose: Establish a known-good baseline. The Phase 1 rewrite will port logic from these files; broken logic would propagate into the new codebase.

Output: Modified versions of main.py, main2.py (deleted), Monte Carlo script, metrics.py, data_loader.py, data_alignment.py, and indicators.py — all passing their verification steps.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@C:\Python Projects\signal_pipeline\VISION.md
@C:\Python Projects\signal_pipeline\.planning\ROADMAP.md
@C:\Python Projects\signal_pipeline\.planning\phases\00-pre-requisite-bug-fixes\RESEARCH.md
</context>

<tasks>

<!-- ═══════════════════════════════════════════════════════════════
     GROUP A — Independent fixes, no dependencies between them.
     Commit together when all five are complete.
     ═══════════════════════════════════════════════════════════════ -->

<task type="auto">
  <name>Task A1: Remove dead code block in main.py (lines 3084-3172)</name>
  <files>composer_signal_generator/main.py</files>
  <action>
Open main.py. Locate the function `build_portfolio_smart_sharpe`. Confirm `return port, R, w` at line 3080 (give or take minor drift). The block beginning at line 3084 with `# === Post-processing for combos...` (or similar heading) through `return all_results` at line 3172 is unreachable dead code inside the function body — it follows a return statement. Delete lines 3084-3172 inclusive.

After deletion, confirm:
- The function body ends cleanly after the return at ~line 3080 and the closing comment `# ---- /SMART SHARPE WEIGHT OPTIMIZATION ----`.
- The next function `fmt_pct` (or whatever follows at ~line 3174) is intact and correctly indented.
- No undefined function calls (`summarize_combo_distributions`, `build_combo_corr`, `portfolio_from_shortlist`, `per_fold_ssopt`, `build_and_write_portfolio_series`) remain in the file at all, or if they do appear elsewhere, confirm they are not inside `build_portfolio_smart_sharpe`.

Do NOT touch any other function. Do NOT remove the `COMBO_MODULES_AVAILABLE` flag or any import — only delete the unreachable block.
  </action>
  <verify>
    <automated>python -c "import ast, sys; ast.parse(open('composer_signal_generator/main.py').read()); print('syntax OK')"</automated>
  </verify>
  <done>File parses without SyntaxError; grep for `summarize_combo_distributions` inside `build_portfolio_smart_sharpe` returns nothing; file is shorter by ~89 lines.</done>
</task>

<task type="auto">
  <name>Task A2: Rename _ATR to _rolling_std and update _ALLOWED_CALLS (Phase A only)</name>
  <files>composer_signal_generator/main.py</files>
  <action>
This is Phase A only — rename and re-register. Do NOT implement true ATR in main.py (that requires OHLC from yfinance, which is an architectural change deferred to Phase 1).

Step 1 — Rename the function definition. At lines 111-115, the function reads:

    def _ATR(prices: pd.DataFrame, tkr: str, n: int) -> pd.Series:
        """Average True Range"""
        s = _PRICE(prices, tkr)
        # Simple approximation using price volatility
        return s.rolling(int(n)).std()

Change to:

    def _rolling_std(prices: pd.DataFrame, tkr: str, n: int) -> pd.Series:
        """Rolling standard deviation of price (previously mislabelled _ATR)."""
        s = _PRICE(prices, tkr)
        return s.rolling(int(n)).std()

Step 2 — Update `_ALLOWED_CALLS` (lines 124-127). Remove both `"ATR": _ATR` and `"atr": _ATR`. Add `"ROLLING_STD": _rolling_std` and `"rolling_std": _rolling_std` in their place. Do NOT add ATR back here — that will be done in Phase 1 when the OHLC data layer exists.

The resulting `_ALLOWED_CALLS` should have ROLLING_STD/rolling_std where ATR/atr previously were, and all other entries unchanged.

Do not change any other caller. No other code in main.py references `_ATR` by name directly — all calls go through `_ALLOWED_CALLS` via `_safe_eval_precond`.
  </action>
  <verify>
    <automated>python -c "import ast, sys; ast.parse(open('composer_signal_generator/main.py').read()); print('syntax OK')"</automated>
  </verify>
  <done>`grep -n "_ATR" composer_signal_generator/main.py` returns zero hits; `grep -n "_rolling_std" composer_signal_generator/main.py` shows both the function definition and its two entries in _ALLOWED_CALLS.</done>
</task>

<task type="auto">
  <name>Task A3: Add _BBAND_LOWER to main.py and register in _ALLOWED_CALLS</name>
  <files>composer_signal_generator/main.py</files>
  <action>
Step 1 — Add the new function immediately after `_BBANDS` (which ends around line 109). Insert:

    def _BBAND_LOWER(prices: pd.DataFrame, tkr: str, n: int, std: float = 2.0) -> pd.Series:
        """Bollinger Bands — returns lower band (SMA - std * rolling_std)."""
        s = _PRICE(prices, tkr)
        sma = s.rolling(int(n)).mean()
        std_dev = s.rolling(int(n)).std()
        return sma - (std_dev * float(std))

Step 2 — Update `_ALLOWED_CALLS` to add both case variants:

    "BBAND_LOWER": _BBAND_LOWER,
    "bband_lower": _BBAND_LOWER,

Do NOT rename `_BBANDS` or remove its entries from `_ALLOWED_CALLS`. Existing precondition expressions that reference `BBANDS(...)` must continue to work — `_BBANDS` stays as-is as a backward-compatible alias for the upper band.

Optionally (matching VISION.md guidance): also add `"BBAND_UPPER": _BBANDS, "bband_upper": _BBANDS` as explicit aliases for clarity. This is additive and does not break anything.
  </action>
  <verify>
    <automated>python -c "import ast, sys; ast.parse(open('composer_signal_generator/main.py').read()); print('syntax OK')"</automated>
  </verify>
  <done>`grep -n "BBAND_LOWER" composer_signal_generator/main.py` shows function definition and at least two entries in _ALLOWED_CALLS; `grep -n "BBANDS" composer_signal_generator/main.py` still shows _BBANDS function and its original _ALLOWED_CALLS entries intact.</done>
</task>

<task type="auto">
  <name>Task A4: Fix Total_Trades bug in rsi_tester metrics.py</name>
  <files>rsi_tester/strategy_engine/src/metrics.py</files>
  <action>
Open metrics.py. Find lines 26-33 (approximately):

    active_days = df[df['signal_active'] == 1]
    total_trades = len(active_days)

    if total_trades > 0:
        win_rate = len(active_days[active_days['strategy_return'] > 0]) / total_trades
        avg_return = active_days['strategy_return'].mean()
        streak_starts = ((df['signal_active'] == 1) & (df['signal_active'].shift(1) != 1)).sum()
        avg_hold_days = total_trades / streak_starts if streak_starts > 0 else 0

Replace this block with:

    active_days = df[df['signal_active'] == 1]
    n_active_days = len(active_days)
    streak_starts = ((df['signal_active'] == 1) & (df['signal_active'].shift(1) != 1)).sum()
    total_trades = streak_starts

    if total_trades > 0:
        win_rate = len(active_days[active_days['strategy_return'] > 0]) / n_active_days
        avg_return = active_days['strategy_return'].mean()
        avg_hold_days = n_active_days / streak_starts if streak_starts > 0 else 0

Key changes:
- `n_active_days` captures the day count (preserves the win_rate denominator semantics: fraction of active days profitable).
- `streak_starts` is computed BEFORE the `if total_trades > 0` guard (so it is always defined).
- `total_trades = streak_starts` (counts trade entries, not active days).
- `win_rate` divides by `n_active_days` (not `total_trades`) — this preserves "fraction of active days profitable" semantics per the RESEARCH.md note.
- `avg_hold_days = n_active_days / streak_starts` (days per trade entry).
- The old `streak_starts` and `avg_hold_days` lines inside the block are removed because they are replaced above.

Do not change anything else in the file. Match existing indentation exactly.
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('rsi_tester/strategy_engine/src/metrics.py').read()); print('syntax OK')"</automated>
  </verify>
  <done>`grep -n "total_trades" rsi_tester/strategy_engine/src/metrics.py` shows `total_trades = streak_starts` (not `len(active_days)`); `grep -n "win_rate" rsi_tester/strategy_engine/src/metrics.py` shows denominator is `n_active_days`.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task A5: Verify Monte Carlo drawdown units at runtime (VERIFY FIRST — then fix or comment)</name>
  <what-built>
No code has been changed for this task yet. This checkpoint runs the Monte Carlo script with a real dataset to determine whether `simulation_results['max_drawdowns']` values are fractional (0.0–1.0) or percent-scale (0–30). The correct fix depends on the actual runtime values.

Background from RESEARCH.md: both `run_monte_carlo_simulation` and `analyze_drawdowns` use the identical formula `(peak - value) / (1 + peak / 100)`, which on percent-scale inputs (e.g., peak=10.0 for 10%) produces approximately-percent-scale outputs (~2.73 for a 7% drop from a 10% peak). The VISION.md bug table says "multiply simulation max_drawdowns × 100" — but this would only be correct if values were fractional. Running the script first is mandatory.
  </what-built>
  <how-to-verify>
1. Run the Monte Carlo script with a test symphony:
   `cd "C:\Python Projects\signal_pipeline" && python "monte_carlo_sim/Monte Carlo walk forward composer working.py"`

2. When the script generates results, inspect the `max_drawdowns` array (add a temporary debug print if needed: after `simulation_results = run_monte_carlo_simulation(...)`, print `simulation_results['max_drawdowns'][:10]`).

3. Check the range of the printed values:
   - If values are in the range **0.0–1.0** (e.g., 0.05, 0.12, 0.23): they are fractional. The VISION.md fix is correct — multiply by 100 before passing to `plot_drawdown_distributions`.
   - If values are in the range **0–30** (e.g., 2.73, 8.4, 15.1): they are already percent-scale. Do NOT multiply by 100. Only add a clarifying comment.

4. Look at the generated drawdown histogram plot (if it appears):
   - Does the red "Actual" drawdown vertical line fall within the histogram distribution?
   - If yes, units are already aligned — no numeric fix needed.
   - If the red line is at (e.g.) 0.15 and the histogram x-axis runs 10–30, units are mismatched.

5. Remove any temporary debug print before proceeding.
  </how-to-verify>
  <resume-signal>
Type one of:
- "fractional: multiply by 100" — units are 0.0-1.0, apply the multiplication fix
- "percent: comment only" — units are already 0-30 percent-scale, add comment only
- Describe what you observed if neither case matches exactly
  </resume-signal>
</task>

<task type="auto">
  <name>Task A5b: Apply Monte Carlo fix based on checkpoint A5 finding</name>
  <files>monte_carlo_sim/Monte Carlo walk forward composer working.py</files>
  <action>
Apply whichever fix the checkpoint A5 finding mandated:

**IF "fractional: multiply by 100" was confirmed:**

In `run_monte_carlo_simulation` (around line 618), locate where `max_drawdowns` is appended to `simulation_results`:
    simulation_results['max_drawdowns'].append(max_drawdown)

Change to:
    simulation_results['max_drawdowns'].append(max_drawdown * 100)

Add an inline comment: `# convert fraction to percent to match analyze_drawdowns units`

**IF "percent: comment only" was confirmed:**

In `plot_drawdown_distributions` (around line 1062), add a comment above the `sns.histplot` call:
    # max_drawdowns values are approximately percent-scale (same formula as analyze_drawdowns).
    # Both use: (peak - value) / (1 + peak / 100) on percent-scale inputs. No unit conversion needed.

Do not change any numeric values in the percent-only case.

Match existing code style. Do not change anything else in the file.
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('monte_carlo_sim/Monte Carlo walk forward composer working.py').read()); print('syntax OK')"</automated>
  </verify>
  <done>File parses without error; the fix (multiplication or comment) matches the checkpoint A5 finding; no other lines changed.</done>
</task>

<!-- Commit point: Group A complete (Tasks A1-A5b) -->
<!-- git commit -m "fix(phase0-groupA): remove dead code, rename _ATR, add _BBAND_LOWER, fix Total_Trades, verify MC drawdown units" -->

<!-- ═══════════════════════════════════════════════════════════════
     GROUP B — Back-port from main2.py. Execute in order.
     Commit together when tasks B1-B3 are all verified.
     ═══════════════════════════════════════════════════════════════ -->

<task type="auto">
  <name>Task B1: Back-port precond_mask threading from main2.py</name>
  <files>composer_signal_generator/main.py</files>
  <action>
This is the most complex change in Phase 0. Four coordinated edits to main.py. Read RESEARCH.md Task 2 section before starting.

**Critical constraints:**
- Do NOT remove the resume/checkpoint system (`completed_evaluations` parameter, pickle resume logic) — this exists in main.py and is absent from main2.py (that's a regression in main2.py).
- Do NOT pass precond_mask to `enrich_with_synergistic_combos` — match main2.py behavior.

**Change 1 — `_run_single_backtest` (around line 4460):**

Current 6-element unpack:
    (signal_name, signal, price_data, target_ticker, daily_ret, EXECUTION_MODE) = args

Replace with 7-element unpack:
    (signal_name, signal, price_data, target_ticker, daily_ret, precond_mask, EXECUTION_MODE) = args

After the existing line `sig = signal.reindex(price_data.index).fillna(False)`, add:
    if precond_mask is not None:
        pc = precond_mask.reindex(price_data.index).fillna(False)
        sig = sig & pc

If there is a docstring comment saying "Pre-filtering is now done in the main process, so this function is simpler" (or similar), remove it — it is no longer accurate.

**Change 2 — `backtest_signals` signature and task tuple (around line 4497):**

Current signature:
    def backtest_signals(signals, price_data, target_tickers, benchmark_data=None, period_name=""):

New signature:
    def backtest_signals(signals, price_data, target_tickers, benchmark_data=None, period_name="", precond_mask=None):

Current task tuple (around line 4507):
    task_args = (signal_name, signal, price_data, target_ticker,
                 daily_ret, EXECUTION_MODE)

New task tuple (insert `precond_mask` before `EXECUTION_MODE`):
    task_args = (signal_name, signal, price_data, target_ticker,
                 daily_ret, precond_mask, EXECUTION_MODE)

**Change 3 — Three runner functions, each gets `precond_mask=None` parameter:**

- `run_walk_forward_evaluation` (around line 2252): add `precond_mask=None` to its parameter list; find all internal calls to `backtest_signals(...)` within this function and add `precond_mask=precond_mask` to each call.
- `run_rolling_window_evaluation` (around line 2415): same.
- `run_expanding_window_evaluation` (around line 2575): same.

**Change 4 — `run_comprehensive_evaluation` (around line 2737):**

Locate the pre-filter block that builds `filtered_signals` (the block that does `s & precond_mask` for each signal, approximately lines 2759-2779). Replace the entire block with code that builds `precond_mask` and passes it through:

Keep the code that builds `precond_mask` from the config (the `build_precondition_series` call or equivalent). Remove the construction of `filtered_signals`. Change all three runner function calls that currently pass `filtered_signals` to pass `signals` (the original unmodified dict) with `precond_mask=precond_mask` as an additional keyword argument.

Also update any direct `backtest_signals(...)` calls inside `run_comprehensive_evaluation` itself (e.g., the holdout section) to include `precond_mask=precond_mask`.

Do not touch the `enrich_with_synergistic_combos` call — it does not receive precond_mask.
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('composer_signal_generator/main.py').read()); print('syntax OK')"</automated>
  </verify>
  <done>
File parses without error.
`grep -n "precond_mask" composer_signal_generator/main.py` shows hits in: _run_single_backtest (unpack + apply), backtest_signals (signature + tuple), all three runner functions (signature + pass-through), run_comprehensive_evaluation (build + pass to runners and holdout backtest_signals).
`grep -n "filtered_signals" composer_signal_generator/main.py` returns zero hits (the pre-filter block is gone).
`grep -n "enrich_with_synergistic_combos" composer_signal_generator/main.py` shows the call does NOT include precond_mask.
  </done>
</task>

<task type="auto">
  <name>Task B2: Make max_workers user-configurable via interactive prompt</name>
  <files>composer_signal_generator/main.py</files>
  <action>
**Step 1 — Add module-level default near the top of the file**, grouped with existing module-level constants (e.g., near `EXECUTION_MODE`, `COMBO_MODULES_AVAILABLE`):

    MAX_WORKERS = 5  # configurable via startup prompt; lower values reduce RAM usage

**Step 2 — Add interactive prompt in `get_enhanced_user_inputs`** (the function that collects all user inputs, around lines 3497-3739). Add a "System Resources" section. Place it near the top of the function body (before the evaluation method selection prompts) so the user sees it early:

    print("\nSystem Resources:")
    max_workers_input = safe_input(
        "Max parallel workers (lower = less RAM; default 5, recommended for 16 GB+ systems): ",
        default="5"
    ).strip()
    config['max_workers'] = int(max_workers_input) if max_workers_input.isdigit() else 5

**Step 3 — Set the global in `enhanced_main` or `run_comprehensive_evaluation`**, after `base_cfg` is loaded from config:

    global MAX_WORKERS
    MAX_WORKERS = base_cfg.get('max_workers', 5)

**Step 4 — Replace hardcoded `max_workers=5` in `backtest_signals`** (around line 4514):

Current:
    with ProcessPoolExecutor(max_workers=5) as executor:

Replace with:
    with ProcessPoolExecutor(max_workers=MAX_WORKERS) as executor:

**Step 5 — Replace hardcoded `max_workers=5` in `enrich_with_synergistic_combos`** (around line 5049):

Current:
    with ProcessPoolExecutor(max_workers=5, initializer=_init_worker, initargs=init_args) as executor:

Replace with:
    with ProcessPoolExecutor(max_workers=MAX_WORKERS, initializer=_init_worker, initargs=init_args) as executor:

That is all five steps. Match existing code style. Do not change anything else.
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('composer_signal_generator/main.py').read()); print('syntax OK')"</automated>
  </verify>
  <done>
`grep -n "MAX_WORKERS" composer_signal_generator/main.py` shows: the module-level default, the `global MAX_WORKERS` assignment, and both ProcessPoolExecutor calls.
`grep -n "max_workers=5" composer_signal_generator/main.py` returns zero hits (no hardcoded 5 remains).
`grep -n "max_workers" composer_signal_generator/main.py` shows the prompt in get_enhanced_user_inputs and config assignment.
  </done>
</task>

<task type="auto">
  <name>Task B3: Delete main2.py</name>
  <files>composer_signal_generator/main2.py</files>
  <action>
Confirm tasks B1 and B2 are complete and verified before proceeding.

Delete the file `composer_signal_generator/main2.py`. Confirm no other file imports from main2 or references it by path:

    grep -rn "main2" composer_signal_generator/ --include="*.py"

If any file references main2, investigate before deleting. Based on RESEARCH.md, no other files reference main2.py, so deletion is safe.

Delete the file. Do not replace it with anything.
  </action>
  <verify>
    <automated>python -c "import os; assert not os.path.exists('composer_signal_generator/main2.py'), 'main2.py still exists'; print('main2.py deleted')"</automated>
  </verify>
  <done>`composer_signal_generator/main2.py` does not exist on the filesystem.</done>
</task>

<!-- Commit point: Group B complete (Tasks B1-B3) -->
<!-- git commit -m "fix(phase0-groupB): back-port precond_mask threading, add max_workers prompt, delete main2.py" -->

<!-- ═══════════════════════════════════════════════════════════════
     GROUP C — Tiingo OHLC chain. Must execute in order: C1 → C2 → C3.
     Commit together when all three are verified.
     OPERATIONAL NOTE: After C1 is complete, delete existing ticker CSVs
     from the rsi_tester data directory before running any tests. Old
     date,close CSVs will not be auto-upgraded — the freshness check
     uses date only, so stale close-only files appear current.
     ═══════════════════════════════════════════════════════════════ -->

<task type="auto">
  <name>Task C1: Extend data_loader.py to download OHLC (adjOpen/adjHigh/adjLow/adjClose)</name>
  <files>rsi_tester/strategy_engine/src/data_loader.py</files>
  <action>
Open data_loader.py. Find `download_ticker_data` (around lines 36-71). Locate the DataFrame construction and column selection block that currently reads:

    df = pd.DataFrame(data)
    df['date'] = pd.to_datetime(df['date']).dt.strftime('%Y-%m-%d')
    df = df[['date', 'adjClose']].rename(columns={'adjClose': 'close'})

Replace with:

    df = pd.DataFrame(data)
    df['date'] = pd.to_datetime(df['date']).dt.strftime('%Y-%m-%d')
    df = df[['date', 'adjOpen', 'adjHigh', 'adjLow', 'adjClose']].rename(
        columns={
            'adjOpen': 'open',
            'adjHigh': 'high',
            'adjLow': 'low',
            'adjClose': 'close',
        }
    )

The rest of `download_ticker_data` (directory creation, file write) is unchanged. The Tiingo API already returns `adjOpen`, `adjHigh`, `adjLow` in its response — no changes to the API call are needed.

`check_freshness_and_update` reads `df['date'].max()` from the CSV — the `date` column exists in both the old 2-column and new 5-column format, so no change is needed there.

OPERATIONAL NOTE FOR EXECUTOR: After this change is saved, manually delete all existing ticker CSV files in the rsi_tester data directory (e.g., `rsi_tester/data/*.csv` or wherever they are stored). The freshness check compares dates, not column counts — it will not detect that old close-only CSVs need upgrading. A fresh download is required for OHLC to be available.
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('rsi_tester/strategy_engine/src/data_loader.py').read()); print('syntax OK')"</automated>
  </verify>
  <done>`grep -n "adjHigh" rsi_tester/strategy_engine/src/data_loader.py` shows the new column selection; `grep -n "adjClose" rsi_tester/strategy_engine/src/data_loader.py` shows it in the rename map only; file parses without error.</done>
</task>

<task type="auto">
  <name>Task C2: Update data_alignment.py to forward OHLC columns from load_ticker_csv</name>
  <files>rsi_tester/strategy_engine/src/data_alignment.py</files>
  <action>
Open data_alignment.py. Two coordinated changes:

**Change 1 — `load_ticker_csv` (lines 5-17):**

Current final line:
    return df[['date', 'close']]

Replace the entire function body's return logic with:

    df = df.sort_values('date').reset_index(drop=True)
    if 'high' in df.columns and 'low' in df.columns and 'open' in df.columns:
        return df[['date', 'open', 'high', 'low', 'close']]
    return df[['date', 'close']]

This preserves backward compatibility: old close-only CSVs return `['date', 'close']` unchanged; new OHLC CSVs return all five columns.

**Change 2 — `build_master_dataframe`:**

Find the section that loads each ticker and renames the `close` column with a role-based prefix (e.g., `rename(columns={'close': 'signal_close'})`). For each ticker load (signal ticker and target ticker), update the rename to also forward OHLC columns when present:

For the signal ticker:
    df_signal = load_ticker_csv(signal_ticker, data_dir)
    rename_map = {'close': 'signal_close'}
    if 'high' in df_signal.columns:
        rename_map.update({'open': 'signal_open', 'high': 'signal_high', 'low': 'signal_low'})
    df_signal = df_signal.rename(columns=rename_map)

For the target ticker (apply the same pattern with 'target_' prefix):
    df_target = load_ticker_csv(target_ticker, data_dir)
    rename_map = {'close': 'target_close'}
    if 'high' in df_target.columns:
        rename_map.update({'open': 'target_open', 'high': 'target_high', 'low': 'target_low'})
    df_target = df_target.rename(columns=rename_map)

Do not change the merge logic, the date alignment logic, or any other part of `build_master_dataframe`. Match existing code style exactly.
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('rsi_tester/strategy_engine/src/data_alignment.py').read()); print('syntax OK')"</automated>
  </verify>
  <done>
`grep -n "signal_high" rsi_tester/strategy_engine/src/data_alignment.py` shows the rename_map update in build_master_dataframe.
`grep -n "return df" rsi_tester/strategy_engine/src/data_alignment.py` shows both the OHLC branch and the close-only fallback in load_ticker_csv.
File parses without error.
  </done>
</task>

<task type="auto">
  <name>Task C3: Add calculate_atr() and calculate_bbands_lower() to indicators.py; wire into add_indicator()</name>
  <files>rsi_tester/strategy_engine/src/indicators.py</files>
  <action>
Open indicators.py. Current functions: `calculate_sma`, `calculate_ema`, `calculate_rsi`, `calculate_cumret`, `add_indicator`.

**Step 1 — Add two new functions after `calculate_cumret` and before `add_indicator`:**

    def calculate_atr(high: pd.Series, low: pd.Series, close: pd.Series, period: int) -> pd.Series:
        """
        Wilder's Average True Range.

        high, low, close must be pd.Series aligned to the same DatetimeIndex.
        Uses Wilder's smoothing (EMA with alpha = 1 / period).
        """
        prev_close = close.shift(1)
        true_range = pd.concat(
            [high - low, (high - prev_close).abs(), (low - prev_close).abs()],
            axis=1,
        ).max(axis=1)
        return true_range.ewm(alpha=1 / period, adjust=False).mean()


    def calculate_bbands_lower(series: pd.Series, period: int, num_std: float = 2.0) -> pd.Series:
        """Lower Bollinger Band: SMA(period) - num_std * rolling_std(period)."""
        sma = series.rolling(window=period).mean()
        std = series.rolling(window=period).std()
        return sma - (std * num_std)

**Step 2 — Update `add_indicator` dispatch.**

`add_indicator` currently dispatches on `indicator_name.upper()` with known names (RSI, SMA, EMA, CUMRET). Add two new elif branches before the final else/ValueError:

    elif indicator_name.upper() == "ATR":
        high_col = f"{asset_role}_high"
        low_col = f"{asset_role}_low"
        close_col = f"{asset_role}_close"
        if high_col not in df.columns or low_col not in df.columns:
            raise ValueError(
                f"ATR requires '{high_col}' and '{low_col}' columns in the DataFrame. "
                f"Delete existing ticker CSVs and re-download with OHLC support (task C1)."
            )
        df[indicator_col] = calculate_atr(df[high_col], df[low_col], df[close_col], period)
    elif indicator_name.upper() == "BBAND_LOWER":
        df[indicator_col] = calculate_bbands_lower(df[price_col], period)

Where `indicator_col` and `price_col` follow the existing pattern in `add_indicator` (e.g., `f"{asset_role}_{indicator_name.lower()}_{period}"` and `f"{asset_role}_close"`). Match exactly how other dispatch branches construct these names.

Do not change any existing dispatch branch. Do not add imports unless `pd` is not already imported (it is).
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('rsi_tester/strategy_engine/src/indicators.py').read()); print('syntax OK')"</automated>
  </verify>
  <done>
`grep -n "calculate_atr\|calculate_bbands_lower" rsi_tester/strategy_engine/src/indicators.py` shows both function definitions and both dispatch branches in add_indicator.
`grep -n "ATR\|BBAND_LOWER" rsi_tester/strategy_engine/src/indicators.py` shows the elif dispatch strings.
File parses without error.
  </done>
</task>

<!-- Commit point: Group C complete (Tasks C1-C3) -->
<!-- git commit -m "fix(phase0-groupC): extend Tiingo data_loader to OHLC, forward OHLC in data_alignment, add ATR and BBAND_LOWER to indicators" -->

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input → max_workers | User enters an integer; used as ProcessPoolExecutor max_workers |
| Tiingo API response → CSV | API JSON fields are trusted (authenticated API key); field names adjOpen/adjHigh/adjLow/adjClose assumed present |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-01 | Tampering | max_workers user input | mitigate | Use `.isdigit()` guard before `int()` conversion; fall back to default 5 on non-integer input |
| T-00-02 | Denial of Service | max_workers set to very large value | accept | This is a local developer tool; no external users; user is trusted |
| T-00-03 | Information Disclosure | precond_mask applied to windowed slices | accept | All data is local; no network exposure |
| T-00-04 | Tampering | Tiingo API field names missing (adjHigh absent) | mitigate | pandas KeyError will surface immediately on first download; clear failure mode |
| T-00-SC | Tampering | npm/pip/cargo installs | accept | Phase 0 has zero new package installs; no dependency changes |
</threat_model>

<verification>
After all tasks are complete and all three group commits are made:

1. **Syntax check all modified Python files:**
   ```
   python -m py_compile composer_signal_generator/main.py
   python -m py_compile monte_carlo_sim/"Monte Carlo walk forward composer working.py"
   python -m py_compile rsi_tester/strategy_engine/src/metrics.py
   python -m py_compile rsi_tester/strategy_engine/src/data_loader.py
   python -m py_compile rsi_tester/strategy_engine/src/data_alignment.py
   python -m py_compile rsi_tester/strategy_engine/src/indicators.py
   ```

2. **main2.py is gone:**
   ```
   python -c "import os; assert not os.path.exists('composer_signal_generator/main2.py')"
   ```

3. **_ATR is fully gone from main.py:**
   ```
   grep -n "_ATR\b" composer_signal_generator/main.py
   ```
   Expected: zero hits (note: `_ATR` as a substring of `_BBAND_LOWER` or similar should not appear either — use word boundary).

4. **precond_mask is threaded (not pre-filtered):**
   ```
   grep -n "filtered_signals" composer_signal_generator/main.py
   ```
   Expected: zero hits.

5. **Total_Trades uses streak_starts:**
   ```
   grep -n "total_trades" rsi_tester/strategy_engine/src/metrics.py
   ```
   Expected: `total_trades = streak_starts` (not `len(active_days)`).

6. **OHLC columns in data_loader:**
   ```
   grep -n "adjHigh" rsi_tester/strategy_engine/src/data_loader.py
   ```
   Expected: the column selection in download_ticker_data.

7. **ATR dispatch in indicators.py:**
   ```
   grep -n "calculate_atr\|BBAND_LOWER" rsi_tester/strategy_engine/src/indicators.py
   ```
   Expected: function definitions and dispatch branches.
</verification>

<success_criteria>
Phase 0 is complete when all 11 success criteria from ROADMAP.md are satisfied:

1. Dead code block removed from main.py — no calls to undefined functions in `build_portfolio_smart_sharpe`
2. `precond_mask` threaded through runner functions and applied inside `_run_single_backtest` per windowed slice
3. `max_workers` read from user prompt; both ProcessPoolExecutor calls respect `MAX_WORKERS`
4. `main2.py` deleted
5. `_ATR()` renamed to `_rolling_std()` in main.py; `_ALLOWED_CALLS` updated (ATR/atr removed, ROLLING_STD/rolling_std added)
6. `_BBAND_LOWER()` added to main.py and registered in `_ALLOWED_CALLS`
7. `data_loader.py` saves `date,open,high,low,close` CSVs; old `date,close` CSVs load without error
8. `load_ticker_csv()` returns OHLC when available; `build_master_dataframe()` emits role-prefixed OHLC columns
9. `calculate_atr()` (Wilder ATR) and `calculate_bbands_lower()` exist in `indicators.py` and are dispatched by `add_indicator()`
10. Monte Carlo drawdown units confirmed at runtime; overlay is visually consistent (fix or comment applied per checkpoint finding)
11. `metrics.py` `Total_Trades` counts 0→1 transitions; `win_rate` denominator remains `len(active_days)`
</success_criteria>

<output>
Create `.planning/phases/00-pre-requisite-bug-fixes/00-01-SUMMARY.md` when done.
</output>
