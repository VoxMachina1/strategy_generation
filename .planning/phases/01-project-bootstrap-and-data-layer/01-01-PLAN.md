---
phase: 01-project-bootstrap-and-data-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - signal_pipeline/src/__init__.py
  - signal_pipeline/src/data/__init__.py
  - signal_pipeline/src/data/loader.py
  - signal_pipeline/src/data/alignment.py
  - signal_pipeline/src/data/cache.py
  - signal_pipeline/src/indicators.py
  - signal_pipeline/src/utils/__init__.py
  - signal_pipeline/.env.example
  - signal_pipeline/.gitignore
  - signal_pipeline/tests/__init__.py
  - signal_pipeline/tests/fixtures/SPY.csv
autonomous: true
requirements:
  - SC-1
  - SC-2
  - SC-3
  - SC-4

must_haves:
  truths:
    - "All src/ directories and __init__.py stubs exist and are importable as Python packages"
    - "from src.data.loader import check_freshness_and_update succeeds when run from signal_pipeline/"
    - "calculate_rsi(close_series, 14) returns NaN for rows 0-13 and values in [0,100] for rows 14+"
    - "build_indicator_cache() with duplicate (ticker, fn, window) entries returns a dict with exactly one entry per unique key"
  artifacts:
    - path: "signal_pipeline/src/__init__.py"
      provides: "src package root"
    - path: "signal_pipeline/src/data/__init__.py"
      provides: "src.data package root"
    - path: "signal_pipeline/src/data/loader.py"
      provides: "load_api_keys, get_latest_tiingo_date, download_ticker_data, check_freshness_and_update"
      exports: ["load_api_keys", "get_latest_tiingo_date", "download_ticker_data", "check_freshness_and_update"]
    - path: "signal_pipeline/src/data/alignment.py"
      provides: "load_ticker_csv, build_master_dataframe, load_multi_ticker_aligned"
      exports: ["load_ticker_csv", "build_master_dataframe", "load_multi_ticker_aligned"]
    - path: "signal_pipeline/src/data/cache.py"
      provides: "build_indicator_cache, _compute_indicator"
      exports: ["build_indicator_cache"]
    - path: "signal_pipeline/src/indicators.py"
      provides: "all 8 calculate_* functions plus add_indicator"
      exports: ["calculate_sma", "calculate_ema", "calculate_rsi", "calculate_cumret", "calculate_atr", "calculate_bbands_lower", "calculate_maxdd", "calculate_mareturn", "add_indicator"]
    - path: "signal_pipeline/src/utils/__init__.py"
      provides: "utils stub package"
    - path: "signal_pipeline/.env.example"
      provides: "API key configuration template"
    - path: "signal_pipeline/tests/fixtures/SPY.csv"
      provides: "Offline SPY price fixture for SC-3 and SC-4"
      contains: "date,open,high,low,close header; minimum 60 rows"
  key_links:
    - from: "signal_pipeline/src/data/cache.py"
      to: "signal_pipeline/src/indicators.py"
      via: "from src.indicators import calculate_* (6 single-series functions + calculate_maxdd + calculate_mareturn)"
      pattern: "from src\\.indicators import"
    - from: "signal_pipeline/src/data/loader.py"
      to: "signal_pipeline/.env"
      via: "load_dotenv(dotenv_path=root / '.env') where root = Path(__file__).resolve().parent.parent.parent"
      pattern: "load_dotenv"
---

<objective>
Create the signal_pipeline/src/ package skeleton and port the Tiingo data layer, indicators,
and indicator cache from rsi_tester/strategy_engine/src/ into new, independent modules.
No imports from rsi_tester/ appear in any src/ file.

Purpose: Phase 1 establishes the foundation all later phases build on. The data layer,
indicator functions, and cache are the three lowest-level primitives. If they are wrong,
every downstream phase inherits the defect.

Output: Seven new files (five modules, two __init__ stubs), one .env.example, one SPY
fixture CSV, and four passing success criteria checks (SC-1 through SC-4).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-project-bootstrap-and-data-layer/RESEARCH.md
</context>

<tasks>

<!-- ═══════════════════════════════════════════════════════════════════
     GROUP A — Create package skeleton and port all modules
     Single logical unit; commit after Task A6 passes verification.
     ═══════════════════════════════════════════════════════════════════ -->

<task type="auto">
  <name>Task A1: Create directory structure and __init__.py stubs</name>
  <files>
    signal_pipeline/src/__init__.py
    signal_pipeline/src/data/__init__.py
    signal_pipeline/src/utils/__init__.py
    signal_pipeline/tests/__init__.py
    signal_pipeline/tests/fixtures/.gitkeep
  </files>
  <action>
    Working directory for all verification commands in this plan: signal_pipeline/

    Create the following directories and files. Every __init__.py is completely empty.

    Directories to create (use os.makedirs or equivalent; they may not exist yet):
      signal_pipeline/src/
      signal_pipeline/src/data/
      signal_pipeline/src/utils/
      signal_pipeline/tests/
      signal_pipeline/tests/fixtures/

    Files to create as empty:
      signal_pipeline/src/__init__.py          — empty
      signal_pipeline/src/data/__init__.py     — empty
      signal_pipeline/src/utils/__init__.py    — empty
      signal_pipeline/tests/__init__.py        — empty
      signal_pipeline/tests/fixtures/.gitkeep  — empty (keeps directory in git)

    Do NOT create signal_pipeline/src/indicators/ as a directory. indicators.py is a
    flat file directly inside src/ (not a sub-package). This is the PLANNING.md layout.
  </action>
  <verify>
    <automated>
      cd signal_pipeline &amp;&amp; python -c "import src; import src.data; import src.utils; print('stubs ok')"
    </automated>
  </verify>
  <done>
    The five paths exist on disk and the import command prints "stubs ok" without error.
  </done>
</task>

<task type="auto">
  <name>Task A2: Write src/data/loader.py</name>
  <files>signal_pipeline/src/data/loader.py</files>
  <action>
    Port download_ticker_data, get_latest_tiingo_date, and check_freshness_and_update
    verbatim from rsi_tester/strategy_engine/src/data_loader.py, then add the new
    load_api_keys() function. Apply the changes listed below — nothing else.

    IMPORTS (the only imports this file needs):
      import os
      import json
      import requests
      import pandas as pd
      from pathlib import Path
      from datetime import datetime, timedelta
      from dotenv import load_dotenv

    Do NOT import from config_loader. Do NOT import from any src.* module.
    Do NOT port the __main__ block.

    NEW FUNCTION — add load_api_keys() before the three ported functions:

      def load_api_keys():
          """
          Loads Tiingo API keys from signal_pipeline/.env.
          Supports comma-separated or JSON array formats for TIINGO_API_KEYS.
          Raises ValueError with a clear message if keys are missing or unparseable.
          """
          root = Path(__file__).resolve().parent.parent.parent  # src/data/ -> src/ -> signal_pipeline/
          load_dotenv(dotenv_path=root / ".env")
          keys_str = os.getenv("TIINGO_API_KEYS")
          if not keys_str:
              raise ValueError(
                  "TIINGO_API_KEYS not found. Create signal_pipeline/.env with: "
                  "TIINGO_API_KEYS=your_key_here"
              )
          if keys_str.strip().startswith("["):
              api_keys = json.loads(keys_str)
          else:
              api_keys = [k.strip() for k in keys_str.split(",") if k.strip()]
          if not api_keys:
              raise ValueError("TIINGO_API_KEYS is set but empty.")
          return api_keys

    PORTED FUNCTIONS — copy from data_loader.py source exactly, with these
    mechanical changes only:
      1. Remove the try/except config_loader import block entirely.
      2. Remove the __main__ block entirely.
      3. Function signatures, docstrings, and bodies are otherwise unchanged.

    The three ported signatures are:
      def get_latest_tiingo_date(api_keys): ...
      def download_ticker_data(ticker, api_keys, data_dir): ...
      def check_freshness_and_update(tickers, api_keys, data_dir): ...

    data_dir must be a pathlib.Path object at all call sites; both download_ticker_data
    and check_freshness_and_update use data_dir / f"{ticker}.csv" which requires Path.
  </action>
  <verify>
    <automated>
      cd signal_pipeline &amp;&amp; python -m py_compile src/data/loader.py &amp;&amp; echo "loader ok"
    </automated>
  </verify>
  <done>
    py_compile exits 0 and prints "loader ok". File contains load_api_keys,
    get_latest_tiingo_date, download_ticker_data, check_freshness_and_update.
    No import from config_loader or from any src.* module.
  </done>
</task>

<task type="auto">
  <name>Task A3: Write src/data/alignment.py</name>
  <files>signal_pipeline/src/data/alignment.py</files>
  <action>
    Port load_ticker_csv and build_master_dataframe verbatim from
    rsi_tester/strategy_engine/src/data_alignment.py, then add the new
    load_multi_ticker_aligned() function. Apply only the changes listed below.

    IMPORTS (the only imports this file needs):
      import pandas as pd
      from pathlib import Path

    Do NOT import os. Do NOT import from any src.* module.
    Do NOT port the __main__ block.

    PORTED FUNCTIONS — copy from data_alignment.py source exactly, with this
    mechanical change only:
      1. Remove the __main__ block entirely.
      2. Function signatures, docstrings, and bodies are otherwise unchanged.

    The two ported signatures are:
      def load_ticker_csv(ticker, data_dir): ...
      def build_master_dataframe(signal_ticker, target_ticker, benchmark_ticker,
                                  data_dir, filter_assets=None): ...

    NEW FUNCTION — add load_multi_ticker_aligned() after build_master_dataframe:

      def load_multi_ticker_aligned(tickers, data_dir):
          """
          Loads each ticker CSV, extracts the close column, renames it to the ticker symbol.
          Aligns all tickers on date via inner join and sorts chronologically.
          Returns a DataFrame with ticker symbols as columns and a datetime index.

          Args:
              tickers:  list of ticker strings, e.g. ["SPY", "QQQ"]
              data_dir: pathlib.Path to the directory containing {ticker}.csv files

          Returns:
              pd.DataFrame with columns = tickers, index = date (DatetimeIndex),
              sorted ascending by date. Inner join means only dates present for ALL
              tickers are included.
          """
          frames = []
          for ticker in tickers:
              df = load_ticker_csv(ticker, data_dir)
              close_series = df.set_index("date")["close"].rename(ticker)
              frames.append(close_series)
          aligned = pd.concat(frames, axis=1, join="inner")
          aligned = aligned.sort_index()
          return aligned

    Note on inner join: pd.concat with join="inner" on a list of Series aligned on their
    index (date) produces the intersection of all date indices. This is the correct
    behaviour — do not use join="outer" or forward-fill.
  </action>
  <verify>
    <automated>
      cd signal_pipeline &amp;&amp; python -m py_compile src/data/alignment.py &amp;&amp; echo "alignment ok"
    </automated>
  </verify>
  <done>
    py_compile exits 0 and prints "alignment ok". File contains load_ticker_csv,
    build_master_dataframe, load_multi_ticker_aligned. No import from any src.* module.
  </done>
</task>

<task type="auto">
  <name>Task A4: Write src/indicators.py</name>
  <files>signal_pipeline/src/indicators.py</files>
  <action>
    Port all six indicator functions and add_indicator from
    rsi_tester/strategy_engine/src/indicators.py, then add calculate_maxdd and
    calculate_mareturn as new functions. Apply only the changes listed below.

    IMPORTS (the only imports this file needs):
      import pandas as pd
      import numpy as np

    CRITICAL — do NOT port these lines that appear in the source file:
      from data_alignment import build_master_dataframe      (try block)
      from .data_alignment import build_master_dataframe     (except block)
    These imports must be completely absent from src/indicators.py. They only existed
    to support the __main__ test block, which is also dropped.

    Do NOT port the __main__ block.

    PORTED FUNCTIONS — copy from indicators.py source exactly, with these
    mechanical changes only:
      1. Remove the try/except build_master_dataframe import block.
      2. Remove the __main__ block.
      3. All six function bodies and add_indicator body are otherwise unchanged.

    The six ported functions:
      def calculate_sma(series, period): ...
      def calculate_ema(series, period): ...
      def calculate_rsi(series, period): ...
      def calculate_cumret(series, period): ...
      def calculate_atr(high, low, close, period): ...
      def calculate_bbands_lower(series, period, num_std=2.0): ...
      def add_indicator(df, asset_role, indicator_name, period): ...

    NEW FUNCTION — add calculate_maxdd() after calculate_bbands_lower,
    before add_indicator. Port EXACTLY from rsi_tester/fuzz_tester.py lines 41-49:

      def calculate_maxdd(series, period):
          """Rolling max drawdown over `period` days (returns positive %, e.g. 15.0 = 15% DD)."""
          def _maxdd(window):
              peak = window.max()
              if peak <= 0:
                  return 0.0
              trough = window.min()
              return ((peak - trough) / peak) * 100
          return series.rolling(window=period).apply(_maxdd, raw=True)

    NEW FUNCTION — add calculate_mareturn() after calculate_maxdd,
    before add_indicator. Write from scratch (no source reference exists):

      def calculate_mareturn(series, period):
          """Rolling mean of daily percentage returns over `period` days."""
          return series.pct_change().rolling(window=period).mean()

    The add_indicator function in the source only dispatches RSI, SMA, EMA, CUMRET,
    ATR, and BBAND_LOWER. Do NOT add MAXDD or MARETURN dispatch to add_indicator in
    Phase 1 — add_indicator is ported unchanged. MAXDD and MARETURN are dispatched
    through build_indicator_cache in cache.py only.
  </action>
  <verify>
    <automated>
      cd signal_pipeline &amp;&amp; python -m py_compile src/indicators.py &amp;&amp; python -c "import subprocess,sys; result=subprocess.run(['grep','-n','build_master_dataframe','src/indicators.py'],capture_output=True,text=True); print('FAIL: forbidden import found' if result.stdout else 'indicators ok')"
    </automated>
  </verify>
  <done>
    py_compile exits 0. grep finds zero occurrences of build_master_dataframe in
    src/indicators.py. File contains all 8 calculate_* functions and add_indicator.
    No import from src.data or any other src.* module.
  </done>
</task>

<task type="auto">
  <name>Task A5: Write src/data/cache.py</name>
  <files>signal_pipeline/src/data/cache.py</files>
  <action>
    Write src/data/cache.py from scratch. This module has no source file to port from;
    it is entirely new.

    IMPORTS:
      import numpy as np
      import pandas as pd
      from src.indicators import (
          calculate_rsi,
          calculate_sma,
          calculate_ema,
          calculate_cumret,
          calculate_atr,
          calculate_bbands_lower,
          calculate_maxdd,
          calculate_mareturn,
      )

    PRIVATE HELPER — write _compute_indicator() exactly as follows:

      def _compute_indicator(series, fn, window, price_df, ticker):
          """
          Dispatches a single-series indicator computation by label.

          Args:
              series:    pd.Series of close prices for the ticker
              fn:        indicator label string (case-insensitive)
              window:    integer lookback period
              price_df:  the full price DataFrame (needed for future ATR dispatch)
              ticker:    ticker symbol string

          Returns:
              pd.Series of computed indicator values

          Raises:
              NotImplementedError: for ATR (Phase 2 will add OHLC dispatch)
              ValueError: for unrecognized indicator labels
          """
          fn_upper = fn.upper()
          if fn_upper == "RSI":
              return calculate_rsi(series, window)
          elif fn_upper == "SMA":
              return calculate_sma(series, window)
          elif fn_upper == "EMA":
              return calculate_ema(series, window)
          elif fn_upper == "CUMRET":
              return calculate_cumret(series, window)
          elif fn_upper == "ATR":
              raise NotImplementedError(
                  "ATR dispatch in build_indicator_cache requires OHLC columns — "
                  "deferred to Phase 2. Use calculate_atr() directly with high/low/close Series."
              )
          elif fn_upper == "BBAND_LOWER":
              return calculate_bbands_lower(series, window)
          elif fn_upper == "MAXDD":
              return calculate_maxdd(series, window)
          elif fn_upper == "MARETURN":
              return calculate_mareturn(series, window)
          else:
              raise ValueError(f"Unknown indicator: {fn!r}")

    PUBLIC FUNCTION — write build_indicator_cache() exactly as follows:

      def build_indicator_cache(price_df, required):
          """
          Pre-computes indicator series for all (ticker, fn_label, window) requests.
          Deduplicates: each unique (ticker, fn_label, window) key is computed exactly once.

          Args:
              price_df:  DataFrame with columns = ticker symbols, index = date (DatetimeIndex).
                         This is the output of load_multi_ticker_aligned().
              required:  List of (ticker, fn_label, window) tuples. May contain duplicates.

          Returns:
              dict mapping (ticker, fn_label, window) -> np.ndarray of shape (n_days,).
              Values are float64. NaN values are preserved (warmup periods, etc.).

          Raises:
              KeyError: if a ticker in required is not a column in price_df
              NotImplementedError: if fn_label is "ATR" (Phase 2)
              ValueError: if fn_label is unrecognized
          """
          cache = {}
          seen = set()
          for ticker, fn, window in required:
              key = (ticker, fn, window)
              if key in seen:
                  continue
              seen.add(key)
              series = price_df[ticker]
              indicator_series = _compute_indicator(series, fn, window, price_df, ticker)
              cache[key] = indicator_series.to_numpy(dtype=float)
          return cache

    No other functions or classes. No __main__ block.
  </action>
  <verify>
    <automated>
      cd signal_pipeline &amp;&amp; python -m py_compile src/data/cache.py &amp;&amp; echo "cache ok"
    </automated>
  </verify>
  <done>
    py_compile exits 0 and prints "cache ok". File contains _compute_indicator and
    build_indicator_cache. ATR raises NotImplementedError. No other public functions.
  </done>
</task>

<task type="auto">
  <name>Task A6: Create .env.example and ensure .env is gitignored</name>
  <files>
    signal_pipeline/.env.example
    signal_pipeline/.gitignore
  </files>
  <action>
    Create signal_pipeline/.env.example with exactly this content:

      # Tiingo API keys for signal_pipeline
      # Get a free key at https://www.tiingo.com/account/api
      #
      # Single key:
      # TIINGO_API_KEYS=your_api_key_here
      #
      # Multiple keys (comma-separated, for rate-limit rotation):
      # TIINGO_API_KEYS=key1,key2,key3
      #
      # Multiple keys (JSON array format, also accepted):
      # TIINGO_API_KEYS=["key1","key2","key3"]
      TIINGO_API_KEYS=your_api_key_here

    If signal_pipeline/.gitignore already exists, append the following lines to it
    (do not overwrite). If it does not exist, create it with these contents:

      # API keys — never commit
      .env

      # Runtime data cache
      data/

      # Python artifacts
      __pycache__/
      *.pyc
      *.pyo

    Verify .env is listed in .gitignore before proceeding to Group B.
  </action>
  <verify>
    <automated>
      cd signal_pipeline &amp;&amp; python -c "content=open('.gitignore').read(); assert '.env' in content, '.env missing from .gitignore'; print('gitignore ok')"
    </automated>
  </verify>
  <done>
    .env.example exists with TIINGO_API_KEYS placeholder. .gitignore contains ".env".
  </done>
</task>

<!-- ═══════════════════════════════════════════════════════════════════
     GROUP B — Verification
     Commit after all four SC checks pass.
     ═══════════════════════════════════════════════════════════════════ -->

<task type="auto">
  <name>Task B1: Create tests/fixtures/SPY.csv</name>
  <files>signal_pipeline/tests/fixtures/SPY.csv</files>
  <action>
    Produce a minimum 60-row SPY fixture CSV at signal_pipeline/tests/fixtures/SPY.csv
    with header: date,open,high,low,close

    Strategy: Check whether signal_pipeline/.env exists and contains a real API key.

    IF signal_pipeline/.env exists and TIINGO_API_KEYS is set to a non-placeholder value
    (i.e., not "your_api_key_here"):
      Download a real SPY slice from Tiingo (startDate=2023-01-01) and write the
      first 100 rows to tests/fixtures/SPY.csv. Use the same HTTP logic as
      download_ticker_data() in loader.py. The CSV must have columns:
      date,open,high,low,close (no index column).

    ELSE (no .env or placeholder key — the common case during bootstrapping):
      Generate synthetic OHLC data programmatically using numpy. Write exactly 90
      rows (enough for RSI(14) to have 76 valid values after warmup). Use this script:

        import numpy as np
        import pandas as pd
        from pathlib import Path

        rng = np.random.default_rng(42)
        n = 90
        dates = pd.date_range("2023-01-03", periods=n, freq="B")  # business days
        close = 400.0 + np.cumsum(rng.normal(0, 2, n))            # random walk around 400
        open_ = close - rng.uniform(0, 1, n)
        high  = close + rng.uniform(0, 2, n)
        low   = close - rng.uniform(0, 2, n)

        df = pd.DataFrame({
            "date":  dates.strftime("%Y-%m-%d"),
            "open":  np.round(open_, 4),
            "high":  np.round(high,  4),
            "low":   np.round(low,   4),
            "close": np.round(close, 4),
        })
        out = Path("tests/fixtures/SPY.csv")
        out.parent.mkdir(parents=True, exist_ok=True)
        df.to_csv(out, index=False)
        print(f"Wrote {len(df)} rows to {out}")

      Run this script from signal_pipeline/ as the working directory.

    All close values must be positive (the random walk starting at 400 with steps
    of std=2 will not go negative over 90 rows).
  </action>
  <verify>
    <automated>
      cd signal_pipeline &amp;&amp; python -c "
import pandas as pd
df = pd.read_csv('tests/fixtures/SPY.csv')
assert list(df.columns) == ['date','open','high','low','close'], f'Bad columns: {df.columns.tolist()}'
assert len(df) >= 60, f'Too few rows: {len(df)}'
assert (df.close > 0).all(), 'Non-positive close prices'
print(f'SPY fixture ok: {len(df)} rows')
"
    </automated>
  </verify>
  <done>
    tests/fixtures/SPY.csv exists with columns [date, open, high, low, close],
    at least 60 rows, all positive close prices.
  </done>
</task>

<task type="auto">
  <name>Task B2: Run SC-1 through SC-4 and confirm all pass</name>
  <files></files>
  <action>
    Run the four success-criterion checks below IN ORDER from signal_pipeline/ as cwd.
    All four must pass. If any fails, diagnose and fix the issue before marking done.
    Do not move on while any check is red.

    SC-1 — All packages importable:
      python -c "
      import src
      import src.data
      import src.data.loader
      import src.data.alignment
      import src.data.cache
      import src.indicators
      import src.utils
      print('SC-1 PASS: all imports ok')
      "

    SC-2 — check_freshness_and_update importable at its exact path:
      python -c "from src.data.loader import check_freshness_and_update; print('SC-2 PASS')"

    SC-3 — RSI(SPY, 14) correctness against fixture:
      python -c "
      import pandas as pd
      import numpy as np
      from src.indicators import calculate_rsi

      df = pd.read_csv('tests/fixtures/SPY.csv')
      close = df['close']
      rsi = calculate_rsi(close, 14)

      # First 14 rows must be NaN (rows 0-13, i.e. iloc[:14])
      assert rsi.iloc[:14].isna().all(), f'Warmup rows not all NaN: {rsi.iloc[:14].tolist()}'

      # All remaining rows must be in [0, 100]
      valid = rsi.iloc[14:].dropna()
      assert (valid >= 0).all() and (valid <= 100).all(), \
          f'RSI out of [0,100] range. Min={valid.min():.2f}, Max={valid.max():.2f}'

      print(f'SC-3 PASS: RSI(14) warmup=14 NaN rows, valid range [{valid.min():.2f}, {valid.max():.2f}]')
      "

    SC-4 — build_indicator_cache deduplication:
      python -c "
      import pandas as pd
      import numpy as np
      from src.data.cache import build_indicator_cache

      # Build a minimal price_df with SPY column from the fixture
      df = pd.read_csv('tests/fixtures/SPY.csv', parse_dates=['date'])
      price_df = df.set_index('date')[['close']].rename(columns={'close': 'SPY'})

      # Pass the same key three times — cache must deduplicate to 1 entry
      required = [('SPY', 'RSI', 14), ('SPY', 'RSI', 14), ('SPY', 'RSI', 14)]
      cache = build_indicator_cache(price_df, required)

      assert len(cache) == 1, f'Expected 1 cache entry, got {len(cache)}'
      key = ('SPY', 'RSI', 14)
      assert key in cache, f'Key {key} not in cache'
      assert isinstance(cache[key], np.ndarray), 'Cache value is not np.ndarray'
      print(f'SC-4 PASS: cache has {len(cache)} entry for 3 duplicate requests')
      "
  </action>
  <verify>
    <automated>
      cd signal_pipeline &amp;&amp; python -c "
import src, src.data, src.data.loader, src.data.alignment, src.data.cache, src.indicators, src.utils
from src.data.loader import check_freshness_and_update
import pandas as pd, numpy as np
from src.indicators import calculate_rsi
from src.data.cache import build_indicator_cache
df = pd.read_csv('tests/fixtures/SPY.csv', parse_dates=['date'])
close = df['close']
rsi = calculate_rsi(close, 14)
assert rsi.iloc[:14].isna().all()
valid = rsi.iloc[14:].dropna()
assert (valid >= 0).all() and (valid <= 100).all()
price_df = df.set_index('date')[['close']].rename(columns={'close': 'SPY'})
cache = build_indicator_cache(price_df, [('SPY','RSI',14),('SPY','RSI',14)])
assert len(cache) == 1
print('SC-1 SC-2 SC-3 SC-4 ALL PASS')
"
    </automated>
  </verify>
  <done>
    All four success criteria pass in a single run. The consolidated verify command
    prints "SC-1 SC-2 SC-3 SC-4 ALL PASS" with exit code 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .env file → load_api_keys() | API key is read from an unvalidated file; malformed content raises ValueError |
| Tiingo HTTPS API → download_ticker_data() | Remote JSON response is deserialized; no schema validation on fields |
| CSV files → load_ticker_csv() | Locally-written CSVs from Tiingo downloads; no external attacker path in Phase 1 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | signal_pipeline/.env | mitigate | .env listed in .gitignore (Task A6); .env.example committed with placeholder only |
| T-01-02 | Tampering | Tiingo CSV files in data/ | accept | data/ is gitignored; files are re-downloaded from authoritative source on staleness; low-value target |
| T-01-03 | Spoofing | TIINGO_API_KEYS in .env | accept | Keys are user-managed credentials; no server-side validation possible in this layer |
| T-01-04 | Denial of Service | get_latest_tiingo_date / download_ticker_data | accept | API key rotation on failure already implemented in source; no retry loop added |
| T-01-SC | Tampering | No new npm/pip/cargo installs in this phase | accept | All dependencies (pandas, numpy, requests, python-dotenv) already installed; no package manager invocations required |
</threat_model>

<verification>
Run from signal_pipeline/ as working directory:

1. All stubs importable:
   python -c "import src; import src.data; import src.utils; print('ok')"

2. Specific import path (SC-2):
   python -c "from src.data.loader import check_freshness_and_update; print('ok')"

3. No forbidden cross-imports in indicators.py:
   python -c "import subprocess; r=subprocess.run(['grep','-n','build_master_dataframe','src/indicators.py'],capture_output=True,text=True); assert not r.stdout, r.stdout; print('no forbidden imports')"

4. RSI warmup and range (SC-3) + cache deduplication (SC-4):
   See Task B2 verify command above.

5. .env protection:
   python -c "assert '.env' in open('.gitignore').read(); print('.env gitignored')"
</verification>

<success_criteria>
SC-1: python -c "import src; import src.data; import src.data.loader; import src.data.alignment; import src.data.cache; import src.indicators; import src.utils" exits 0

SC-2: python -c "from src.data.loader import check_freshness_and_update; print('ok')" exits 0

SC-3: calculate_rsi(close, 14) on the SPY fixture returns NaN for iloc[:14] and values in [0, 100] for all non-NaN rows.

SC-4: build_indicator_cache(price_df, [("SPY","RSI",14),("SPY","RSI",14)]) returns a dict with exactly 1 entry.

All four must be green. No SC may be skipped or deferred.
</success_criteria>

<output>
After all tasks are complete and SC-1 through SC-4 pass:

Commit Group A files with message:
  feat(phase1-groupA): create src/ package structure, port data layer and indicators

Commit Group B files with message:
  feat(phase1-groupB): add SPY test fixture and run SC-1 through SC-4 verification

Then create .planning/phases/01-project-bootstrap-and-data-layer/01-01-SUMMARY.md
using the template at $HOME/.claude/gsd-core/templates/summary.md
</output>
